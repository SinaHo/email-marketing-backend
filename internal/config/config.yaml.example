server:
  port: 50051

database:
  driver: "postgres"
  datasource: "host=localhost port=5432 user=postgres password=postgres dbname=mydb sslmode=disable"

redis:
  addr: localhost:6379
  password: ""
  db: 0

  
logging:
  level: "debug"
  format: "json"  # or "console"

auth:
  jwt_secret: "supersecretkey123"

